export enum ETLResult {
  SUCCESS = 'success',
  ERROR = 'error',
}

export enum ResultLLMType {
  CHAT = 'chat',
  IMAGE = 'image',
  AUDIO = 'audio',
  VIDEO = 'video',
  DOCUMENT = 'document',
  OTHER = 'other',
  ETL = 'etl',
}
