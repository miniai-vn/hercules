export type TUserProfile = {
  first_name: string;
  last_name: string;
  profile_pic: string;
  id: string;
  name: string;
};

export type TFacebookUser = {
  id: string;
  name: string;
  email?: string;
};
